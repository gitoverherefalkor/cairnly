# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
.superpowers/

# Local archive of old handoffs / backups
/_archive/

# Ad-hoc production data dumps (contain user data — never commit)
scratchpad_n8n/
scratchpad*/
exec_*.json

# User report archives must never enter git history
handoff/**/*.pdf
handoff/*.pdf
*.pdf

# One-off design drops should never be committed at root
/*.zip
*.plugin
